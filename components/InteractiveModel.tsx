
import React from 'react';

// This component has been disabled as per request to remove the 3D Bolt model.
// Returning null ensures no black box or unwanted elements are rendered.
export const InteractiveModel = () => {
  return null;
};
